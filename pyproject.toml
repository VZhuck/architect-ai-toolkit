[project]
name = "architect-ai-toolkit"
version = "0.1.0"
description = "Solution Architecture PoC - Python Dependencies"
requires-python = ">=3.10"
dependencies = [
    "pypandoc>=1.11",
    "langchain-text-splitters>=0.3.0",
    "beautifulsoup4>=4.12",
    "lxml>=5.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.uv]
package = false
