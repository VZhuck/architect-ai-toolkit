Skill is design to support managing NFR

## Skills System
- archy-update-
- 

## Taxonomy
- Closed NFR vocabularies (business drivers, quality attributes, ...) are read from `ai-workflow/nfr-taxonomy/`.
- That folder is produced by the `archy-init-nfr-taxonomy` skill, which copies its versioned `taxonomy/` files into the project. Run it first if the folder is missing.

## References
- Manage NFR - knows how to work with nfr file and how to update it. 
  -  args: Business Goal(s), Quality Attribute Requirements, Constrain & Assumptions
  -  Templates: NFR document.md
